$schema: http://json-schema.org/schema#
additionalProperties: false
properties:
  orderId:
    type: string
  encryptedToken:
    type: string
    description: "encrypted credit card token if the customer wants to use token to pay"
required:
- orderId
type: object
